<%= form_with(model: plant) do |form| %>
  <% if plant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plant.errors.count, "error") %> prohibited this plant from being saved:</h2>

      <ul>
        <% plant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :scientific_name %>
    <%= form.text_field :scientific_name %>
  </div>

  <div class="field">
    <%= form.label :average_height %>
    <%= form.text_field :average_height %>
  </div>

  <div class="field">
    <%= form.label :life_cycle %>
    <%= form.text_field :life_cycle %>
  </div>

  <div class="field">
    <%= form.label :is_consumable %>
    <%= form.check_box :is_consumable %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
